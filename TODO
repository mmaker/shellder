* implement split() function in utils.c
* see manpage 3 for dup, exec and discriminate for necessary informations for a
  proc_t structure, then declare it to process.h
* implement a wrapper around fprintf using snprintf over stdout, and replace
  exhisting calls.
* create a run() function in process.c which makes a fork(), then pipe() and
  dup() over the standard output of the parent process and the log file.
